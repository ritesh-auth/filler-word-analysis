# Filler Word Analysis

A Simple algorithm to calculate no of filler words used in an sentence and calculate the percentage

# Requirements

Use the package manager [pip](https://pip.pypa.io/en/stable/) to install rich

```bash
    pip install rich
```
